import React from 'react';

const Admin: React.FC = () => {
  // Toda a lógica foi movida para Dashboard.tsx para unificar a experiência do admin.
  // A rota /admin agora renderiza o componente Dashboard. Este componente não é mais usado.
  return null;
};

export default Admin;